import { ProfileComponent } from "@/components/profile";
import React from "react";

const Profile = () => {
    const currentId = "";
    return <ProfileComponent id={currentId} />;
};

export default Profile;
